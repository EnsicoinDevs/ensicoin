syntax = "proto3";

import "types.proto";

message MiningRequest {
  BlockTemplate template = 1;
  repeated Tx initial_pool = 2; 
}

message MinerResponse {}

service Miner {
  rpc Mine(stream MiningRequest) returns (MinerResponse) {}
  rpc AddTx(stream Tx) returns (MinerResponse) {}
}
