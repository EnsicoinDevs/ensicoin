syntax = "proto3";

import "types.proto";

enum Error {
    INVALID_DATA = 0;
}

message NodeResponse {
  Error error = 1;
}

service Node {
  rpc PublishRawTx(RawTx) returns (NodeResponse) {}
  rpc PublishRawBlock(RawBlock) returns (NodeResponse) {}
}
