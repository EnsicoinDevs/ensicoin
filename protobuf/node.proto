syntax = "proto3";

import "types.proto"

enum Error {
    INVALID_DATA = 0;
}

message Response {
  Error error = 1;
}

service Node {
  rpc PublishRawTx(RawTx) returns (Response) {}
  rpc PublishRawBlock(RawBlock) returns (Response) {}
}
